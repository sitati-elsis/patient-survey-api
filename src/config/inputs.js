const inputs = {
  text: "Simple text field",
  comment: "Simple text area field",
  boolean: "Simple boolean field",
  rating: "Simple min-max rating field",
  checkbox: "Simple checkbox field",
  radiogroup: "Radio group field",
  matrix: "Matrix of radio groups",
};

const inputTypes = Object.keys(inputs);

module.exports = {
  inputTypes,
};
